# Google Calendar API Configuration
# Get these credentials from Google Cloud Console
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google Calendar API
# 4. Create credentials (API Key and OAuth 2.0 Client ID)

# Google API Key (for public API access)
VITE_GOOGLE_API_KEY=your_google_api_key_here

# Google OAuth 2.0 Client ID (for user authentication)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# Optional: Calendar ID (defaults to 'primary' for user's main calendar)
VITE_GOOGLE_CALENDAR_ID=primary
